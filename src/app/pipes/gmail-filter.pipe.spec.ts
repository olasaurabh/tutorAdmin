import { GmailFilterPipe } from './gmail-filter.pipe';

describe('GmailFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GmailFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
